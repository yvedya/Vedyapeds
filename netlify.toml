# Netlify configuration
# - The published website is the contents of the "site" folder (index.html).
# - Serverless functions live in "netlify/functions".
[build]
  publish = "site"
  functions = "netlify/functions"

# Email a backup every Monday at 13:00 UTC
[functions."backup"]
  schedule = "0 13 * * 1"
